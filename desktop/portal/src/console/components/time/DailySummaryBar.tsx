// Task 3 stub. Task 4 replaces the body, adds the pure slot math + tests.
// The `secondsWorked` prop name is the contract — keep it exactly.
export function DailySummaryBar({ secondsWorked }: { secondsWorked: number }) {
  void secondsWorked;
  return <div data-testid="daily-bar" />;
}
