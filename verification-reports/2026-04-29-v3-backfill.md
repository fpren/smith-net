# v3 Backfill Regression Check — 2026-04-29

**Branch:** feat/relay-hetzner-postgres  
**Baseline commit:** 3ba1138  
**Checked at:** 2026-04-29

---

## New commits since 3ba1138

OK — `git log --oneline 3ba1138..HEAD` produced no output. HEAD is at 3ba1138.

## Backfill flag intact

OK — `UserPreferences.kt:575`: `KEY_LIFECYCLE_BACKFILL_DONE = "lifecycle_backfill_done_v3"` (still v3, not bumped).  
OK — `MainActivity.kt:353`: `LaunchedEffect(Unit)` early-returns via `isLifecycleBackfillDone()`.  
OK — `MainActivity.kt:398`: `setLifecycleBackfillDone()` called after successful backfill.

## Dashboard wiring intact

OK — `DashboardViewModel.kt:144-149`: `getMinutesWorkedToday()` calls `getTodayStart()` and applies
`maxOf(e.clockInTime, todayStart)` clamping.  
OK — `DashboardScreen.kt:437-438`: `nowMs` referenced (forces recompose) then `viewModel.getMinutesWorkedToday()` called.

## Invoice fix intact

OK — `JobBoardViewModel.kt:948`: `hourlyRate = if (job.hourlyRate > 0) job.hourlyRate else 85.0` present.

## Test results

BLOCKED — Android SDK not present in this environment (`ANDROID_HOME` unset, `local.properties` absent,
no `adb` binary found). JDK 21 available; JDK 17 not installed. Gradle resolved to 8.2 but failed
immediately on SDK location check. **Tests could not execute; 55-test pass claim unverified by this run.**

## Anomalies

1. **Test suite unrunnable in CI environment**: Android SDK must be installed and `ANDROID_HOME` set
   (or `sdk.dir` added to `android/local.properties`) before `./gradlew :app:testDebugUnitTest` can run.
   This is an environment gap, not a code regression — all source-level checks passed.
2. **JDK 17 absent**: Only JDK 21 found at `/usr/lib/jvm/java-21-openjdk-amd64`. Instructions called
   for JDK 17; note for environment provisioning.
