# GitHub Actions Workflow Hint for APK Builds
# Place this in .github/workflows/android-build.yml

name: Android CI with AI Features

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Cache Gradle packages
      uses: actions/cache@v3
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build debug APK with AI features
      run: ./gradlew assembleDebug

    - name: Upload debug APK
      uses: actions/upload-artifact@v3
      with:
        name: app-debug
        path: app/build/outputs/apk/debug/app-debug.apk

    # Optional: Build release APK with signing
    # - name: Build release APK
    #   run: ./gradlew assembleRelease
    #   env:
    #     ANDROID_KEYSTORE_PATH: ${{ secrets.KEYSTORE_PATH }}
    #     ANDROID_KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
    #     ANDROID_KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
    #     ANDROID_KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
